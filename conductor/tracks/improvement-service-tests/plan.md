# Plan: Expand Service Layer Tests

## Goal
Increase the reliability of core business logic by implementing comprehensive unit tests for the service layer.

## Tasks
1. [ ] Audit existing tests in `tests/` directory.
2. [ ] Create `tests/unit/test_contact_service.py`.
3. [ ] Create `tests/unit/test_pipeline_service.py`.
4. [ ] Ensure high coverage of edge cases (e.g., missing data, deduplication logic).
